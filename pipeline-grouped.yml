steps:
  - label: "Command Parallelised"
    command: "true"
    parallelism: 2

  - group: "Command Parallelised"
    steps:
      - label: "Pass 1"
        command: "true"
      - label: "Pass 2"
        command: "true"

  - group: Uploads ABC
    steps:
      - label: Upload A
        command: buildkite-agent pipeline upload a.yml
      - label: Upload B
        command: buildkite-agent pipeline upload b.yml
      - label: Upload C
        command: buildkite-agent pipeline upload c.yml

  - label: Upload A Parallelised
    command: buildkite-agent pipeline upload a.yml
    parallelism: 2
